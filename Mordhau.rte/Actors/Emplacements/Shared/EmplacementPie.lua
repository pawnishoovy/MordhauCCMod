function Upright(actor)

		actor.Pos = self.Pos + Vector(0,-10)
		actor.RotAngle = 0
		
--[[
		if self.RotAngle > 0 then
			self.reloadNum = self.reloadNum +1/self.Mass;
			self.reloadNum = self.reloadNum -1/self.Mass;
--]]
end


